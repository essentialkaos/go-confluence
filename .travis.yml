sudo: false

language: go

go:
  - 1.8.x
  - 1.9.x
  - 1.10.x
  - tip

branches:
  only:
  - master
  - develop

os:
  - linux

matrix:
  fast_finish: true
  allow_failures:
    - go: tip

before_install:
  - make deps-test

script:
  - make test
